import { TableTitlesDirective } from './table-titles.directive';

describe('TableTitlesDirective', () => {
  it('should create an instance', () => {
    const directive = new TableTitlesDirective();
    expect(directive).toBeTruthy();
  });
});
